<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:tools="http://schemas.android.com/tools"
    package="com.noobshubham.gostore">



    <application
        tools:replace="icon, label"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/grocery"
        android:label="Grocery"
        android:roundIcon="@drawable/grocery"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoStore"
        tools:targetApi="31"
        tools:ignore="android:label">
        <activity android:name=".grocery.GroceryActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>